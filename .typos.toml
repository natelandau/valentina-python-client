[default]
    locale = "en-us"

    [default.extend-words]
        criticals = "criticals"

[files]
    extend-exclude = ["*_cache", ".env", ".env.secret", ".venv", "CHANGELOG.md", "_site", "static"]
